import React from 'react';
import {View,Text,useColorScheme,StyleSheet, Dimensions} from 'react-native'

const Home = () => {
    return (
        <View>
        
        </View>
    );
};

export default Home;